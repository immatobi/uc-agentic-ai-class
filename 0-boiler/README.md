# Learner Support

Python project scaffolded with `uv` using a flat `/src` app layout for LangGraph-based learner support experiments.

## Setup

```bash
uv sync
cp .env.example .env
```

## Run

```bash
uv run src/main.py
```
